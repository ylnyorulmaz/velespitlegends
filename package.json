{
  "name": "velespitlegends",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix backend --omit=dev",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
