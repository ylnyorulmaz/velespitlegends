{
  "name": "velespitlegends",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix backend --omit=dev && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
